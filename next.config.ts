import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  pageExtensions: ["js", "jsx", "ts", "tsx"], // Ensure TypeScript files are recognized
};

export default nextConfig;
